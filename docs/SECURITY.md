# Security policy

Do not report suspected vulnerabilities through public issues. Configure this document with the
organization's private reporting channel, response targets, supported versions, and disclosure
policy before the template is used for a production repository.

Security-sensitive changes should receive review from both the owning component team and the
organization's security engineering function.
